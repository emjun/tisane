install.packges("multicore")

#install.packages("simgen_1.54.tar.gz", repos=NULL) # Change to path where tarball is installed
install.packages("~/R/simgen_1.54.tar.gz", repos=NULL) # Change to path where tarball is installed

library(simgen)

# TODO: START HERE: Copy paste the code for generating the data; open 24.csv