tisane.SetUp
============

.. currentmodule:: tisane

.. autoclass:: SetUp
   :show-inheritance:

   

   .. HACK -- the point here is that we don't want this to appear in the output, but the autosummary should still generate the pages.
      .. autosummary::
         :toctree:
      
         SetUp.add_data
         SetUp.assign_cardinality_from_data
         SetUp.associates_with
         SetUp.calculate_cardinality_from_data
         SetUp.causes
         SetUp.get_cardinality
         SetUp.moderates



   

   .. HACK -- the point here is that we don't want this to appear in the output, but the autosummary should still generate the pages.
      .. autosummary::
         :toctree:
      
         SetUp.variable
         SetUp.name
         SetUp.data
         SetUp.relationships

